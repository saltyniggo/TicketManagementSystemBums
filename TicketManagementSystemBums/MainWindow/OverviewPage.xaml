<Page x:Class="TicketManagementSystemBums.MainWindow.OverviewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TicketManagementSystemBums.MainWindow"
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="800"
      Title="OverviewPage"
      Background="Transparent">

    <Page.FontFamily>
        <FontFamily>Montserrat</FontFamily>
    </Page.FontFamily>
    <DockPanel>
        <Grid DockPanel.Dock="Left"
              Background="#C2DAFF"
              Width="135">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="sidebarTitle"
                       Grid.Row="0"
                       Padding="5"
                       Foreground="#16161D"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontSize="18"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>
            <Label Content="Placholder Link"
                   Grid.Row="1"
                   Foreground="#16161D"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   FontSize="16"/>
            <Label Content="Placholder Link"
                   Grid.Row="2"
                   Foreground="#16161D"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   FontSize="16"/>
            <Label Content="Placholder Link"
                   Grid.Row="3"
                   Foreground="#16161D"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   FontSize="16"/>
            <Button Content="New Button"
                    Grid.Row="5"
                    Width="75"
                    Height="75"
                    Margin="0,0,0,10"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="12">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="45"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="Add Ticket"
                    Grid.Row="6"
                    Width="75"
                    Height="75"
                    Margin="0,0,0,10"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Click="OpenAddTicketForm">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="45"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
        <Grid DockPanel.Dock="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0"
                  Background="#FFAF0F">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" 
                       HorizontalAlignment="Center"
                       Foreground="#16161D"
                       FontSize="20"
                       FontWeight="Bold"
                       Content="Unassigned"/>
                <ListBox Grid.Row="1"
                         x:Name="listUnassigned"
                         Background="#FEFEFE"
                         BorderBrush="#FFAF0F"
                         BorderThickness="0,2,0,0"
                         Foreground="#16161D">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TicketName}" 
                                       MouseDown="Ticket_MouseDown">
                                <Popup Name="DetailsPopup" 
                                       Placement="Right" 
                                       IsOpen="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding TicketDate}"/>
                                        <TextBlock Text="{Binding Priority}"/>
                                        <TextBlock Text="{Binding TicketAssignedUser}"/>
                                        <TextBlock Text="{Binding TicketDescription}"/>
                                    </StackPanel>
                                </Popup>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
            <Grid Grid.Column="1"
                  Background="#FFAF0F">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" 
                       HorizontalAlignment="Center"
                       Foreground="#16161D"
                       FontSize="20"
                       FontWeight="Bold"
                       Content="Assigned"/>
                <ListBox Grid.Row="1"
                         x:Name="listAssigned"
                         Background="#FEFEFE"
                         BorderBrush="#FFAF0F"
                         BorderThickness="2,2,0,0"
                         Foreground="#16161D">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TicketName}" 
                                       MouseDown="Ticket_MouseDown">
                                <Popup Name="DetailsPopup" 
                                       Placement="Right" 
                                       IsOpen="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding TicketDate}"/>
                                        <TextBlock Text="{Binding Priority}"/>
                                        <TextBlock Text="{Binding TicketAssignedUser}"/>
                                        <TextBlock Text="{Binding TicketDescription}"/>
                                    </StackPanel>
                                </Popup>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
            <Grid Grid.Column="2"
                  Background="#FFAF0F">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0"
                       HorizontalAlignment="Center"
                       Foreground="#16161D"
                       FontSize="20"
                       FontWeight="Bold"
                       Content="Completed"/>
                <ListBox Grid.Row="1"
                         x:Name="listCompleted"
                         Background="#FEFEFE"
                         BorderBrush="#FFAF0F"
                         BorderThickness="2,2,0,0"
                         Foreground="#16161D">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TicketName}" 
                                       MouseDown="Ticket_MouseDown">
                                <Popup Name="DetailsPopup" 
                                       Placement="Right" 
                                       IsOpen="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding TicketDate}"/>
                                        <TextBlock Text="{Binding Priority}"/>
                                        <TextBlock Text="{Binding TicketAssignedUser}"/>
                                        <TextBlock Text="{Binding TicketDescription}"/>
                                    </StackPanel>
                                </Popup>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>
    </DockPanel>
</Page>
